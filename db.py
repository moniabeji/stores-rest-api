from flask_sqlalchemy import SQLAlchemy

db = SQLAlchemy() #an object that is going to link to our app, its going to look  at all  of the objects, then it goig$ng to allow us to map those objects
# to rows to a database
